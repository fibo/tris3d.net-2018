# tris3d.net

> Play tic tac toe in 3d

## Deploy

**Only once** install Heroku CLI, login and add *heroku* git remote

```bash
git remote add heroku https://git.heroku.com/tris3d.git
```

From now on you can deploy with

```bash
git push heroku master
```

